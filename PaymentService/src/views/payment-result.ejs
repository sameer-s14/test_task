<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Result</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 50px;
        }
        .success {
            color: green;
        }
        .fail {
            color: red;
        }
        .btn {
            padding: 10px 20px;
            background-color: #4CAF50;
            color: white;
            border: none;
            cursor: pointer;
        }
        .btn:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <h1>Payment Status</h1>
    <p>Order ID: <strong><%= orderId %></strong></p>
    <% if (status === 'success') { %>
        <p class="success">Payment was successful! Thank you for your purchase.</p>
    <% } else { %>
        <p class="fail">Payment failed! Please try again.</p>
    <% } %>
    <form action="/<%= orderId %>" method="GET">
        <button class="btn" type="submit">Go Back</button>
    </form>
</body>
</html>
